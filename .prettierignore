# Owned and rewritten by Next.js on every dev/build run; its quote style is
# not ours to enforce ("This file should not be edited").
apps/*/next-env.d.ts

# Generated and normalized by pnpm; formatting it creates noisy lockfile-only diffs.
pnpm-lock.yaml
