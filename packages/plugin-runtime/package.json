{
  "name": "@videodip/plugin-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Executes VideoDip plugins against the ADR-0009 contract: a pure, channel-injected broker plus a sandboxed-iframe host adapter.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup && tsc --emitDeclarationOnly",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@videodip/plugin-sdk": "workspace:*",
    "@videodip/shared": "workspace:*",
    "@videodip/template-engine": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "rimraf": "6.1.2",
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
