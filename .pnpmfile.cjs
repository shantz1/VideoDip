/**
 * pnpm hooks.
 *
 * Reserved for dependency reconciliation that cannot be expressed in
 * `pnpm-workspace.yaml`. Keep this file empty unless there is a specific,
 * commented reason — silent dependency rewriting is very hard to debug.
 */
module.exports = {
  hooks: {},
};
